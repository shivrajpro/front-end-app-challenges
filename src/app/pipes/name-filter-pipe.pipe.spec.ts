import { NameFilterPipePipe } from './name-filter-pipe.pipe';

describe('NameFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NameFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
